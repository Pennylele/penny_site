from django.db import models

# Create your models here.
# class Blog(modules.Module):
# 	created_at = modules.DateTimeField(auto_now_add=True)
#	title = modules.CharField(max_length=255)
#	body = modules.TextField()

#	def __str__(self):
#		return self.title